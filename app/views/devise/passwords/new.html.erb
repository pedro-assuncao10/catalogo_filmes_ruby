<h1 class="page-title">Recuperar Senha</h1>

<section class="panel">
  <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: "form form-compact" }) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>

    <div class="field">
      <label class="label" for="user_email">Email</label>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "input" %>
    </div>

    <div class="actions">
      <%= f.submit "Enviar instruções", class: "btn btn-brand" %>
      <%= link_to "Voltar ao login", new_session_path(resource_name), class: "btn btn-ghost" %>
    </div>
  <% end %>
</section>
